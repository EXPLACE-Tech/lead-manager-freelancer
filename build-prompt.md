# lead-manager 構築プロンプト

以下をそのまま Claude Code に投げてください。

---

## 依頼内容

`C:\Users\user\dev\projects\client-work\lead-manager` に、案件獲得の営業パイプライン管理ツールを構築してください。

### 目的

案件サイトからの配信メール（Gmail）を起点に、「応募 → 返信 → 商談 → 成約」のプロセスを一元管理し、
**どんな応募文を送ると返信・商談化しやすいか／商談でどう提示すると成約しやすいか** のノウハウを、
成功率の推移として蓄積・考察できるようにする。管理は Google スプレッドシート上で行う。

### 対象サイト（今回は発注ナビのみ）

まずは発注ナビだけを対象に実装する。他サイト（クラウドワークス・Upwork等）は将来追加する前提で、
**スプレッドシートはプラットフォームごとにシート（タブ）を分ける構造**にしておくこと
（サイトによってメールが届くタイミング・トリガーの起点が異なるため。例: Upworkは相手からの配信メールが無く、
こちらが案件をサイト上で見つけて応募文を送付した時点が管理の起点になる、といった違いがある。
今回は実装しないが、後で「Upwork_案件リスト」のようなシートを同じ雛形で追加できるようにしておく）。

**発注ナビ**（1通＝1案件、送信元 `mo-hnavi@ml.itmedia.co.jp`）
```
【発注ナビ】「コーポレートサイトのリニューアル制作」が配信されました
新規案件が配信されました。
URL： https://developer.hnavi.co.jp/jobs/18159
案件No.：202608280013
案件タイトル：コーポレートサイトのリニューアル制作
カテゴリ：開発・制作（ホームページ）
```

### 取り込み方式（完全自動）

- Google Apps Script（GAS）で Gmail を **15分ごと** に監視するタイムトリガーを設定
- 送信元 `mo-hnavi@ml.itmedia.co.jp` からの未処理メールを対象にする
- 新着案件を検知したらパースして「発注ナビ_案件リスト」シートに1行追記し、ステータスを「配信検知」にする
- 処理済みメールは Gmail 上でラベル付け等をして再処理を防ぐこと
- 認証はユーザー自身の Google アカウントの OAuth 同意で完結させる（サービスアカウント等は不要）。初回のデプロイ・トリガー設定・権限付与の手順書も作成すること

### スプレッドシート構成（新規作成、ファイル名・保存場所はお任せ）

プラットフォームごとにシート（タブ）を分ける。今回作るのは発注ナビ分の3シートのみ。

**シート1: 発注ナビ_案件リスト**
配信日時 / 案件No / 案件タイトル / 元カテゴリ表記 / 正規化カテゴリ / 予算 / 応募期限 / URL / ステータス（プルダウン） / 個別カスタム文（案件固有の文章のみをここに書く） / 応募日 / 返信日 / 商談日 / 成約日 or 失注日 / 備考

**シート2: 発注ナビ_応募文テンプレート**
- カテゴリ別に定型文パーツを管理（正規化カテゴリごとに1セット）
- テンプレートを変更したら、変更履歴が別領域（または別タブ）に日付つきで積み上がっていく構造にする（何をいつどう変えたか後から追えるように）
- 実際に送る応募文は「カテゴリ別定型文」＋「案件リストの個別カスタム文」を組み合わせて作る運用。この組み合わせ作業自体は自動生成せず、人間（社長）が都度確認して仕上げる

**シート3: 発注ナビ_集計**
正規化カテゴリ別に、以下3つの転換率を算出する：
1. **返信率**：応募済以降の件数のうち、返信ありに進んだ件数の割合
2. **商談化率**：返信あり以降の件数のうち、商談設定に進んだ件数の割合
3. **成約率**：商談実施済以降の件数のうち、成約に進んだ件数の割合

関数（COUNTIFS等）かピボットテーブルで自動集計し、手動での考察を書き込む欄も設ける。

### ステータス（7段階、プルダウン）

配信検知 → 応募済 → 返信あり → 商談設定 → 商談実施済 → 成約 ／ 失注

### カテゴリ正規化

発注ナビの表記（例:「開発・制作（ホームページ）」）を、少数の統一カテゴリにマッピングする。
叩き台として以下を提案するので、必要に応じて調整してよい：
`Web制作 / システム開発 / デザイン / ライティング / マーケティング / その他`

分類はキーワードマッチングのルールベースで行い（LLM API は使わない — 下記ポリシー参照）、判定できない場合は「未分類」として人間が後で手動修正する前提でよい。

### 明示的にやらないこと

- 案件詳細ページのスクレイピング（予算・要件などはメール本文の情報のみで管理する）
- 新着・ステータス変化の外部通知（Slack/LINE等）は不要
- 応募文の完全自動生成（人間が最終確認・仕上げする運用）
- 発注ナビ以外のサイト対応（シート構造だけ拡張しやすくしておき、実装は将来）

### 環境ポリシー（必ず守ること）

- **Anthropic API / `anthropic` SDK / `ANTHROPIC_API_KEY` は使わない**。カテゴリ分類等の「推論」が必要な処理は本ツールでは発生させない設計にする（ルールベースで完結させる）
- コード・コメントは英語、シート上のUIテキスト（列名・ステータス選択肢等）は日本語
- git の commit / push は明示依頼があるまで行わない
- `docs/進捗.md` を作成し、現在地・Next actions・Blockers・関連パス（作成したスプレッドシートのURL含む）を記録すること
- `client-work/README.md` の案件一覧表に `lead-manager` の行を追記すること（このツールは受託先向けではなく自社の営業パイプライン管理ツールである旨を明記）

### 成果物

1. GAS スクリプト一式（Gmail監視・パース・シート書き込み、サイトを追加しやすいよう構造を分離）
2. 新規作成したスプレッドシート（発注ナビ分の3シート構成、ステータスのプルダウン設定済み、集計式設定済み）
3. 初回セットアップ手順書（GASのデプロイ方法・トリガー設定・OAuth権限付与）
4. `docs/進捗.md`

不明点があれば実装前に確認してください。
